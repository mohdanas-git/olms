<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User's Article</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
    <link rel="stylesheet" href="style.css">
    <style>
        #book-img{
            width: 100%;
        }
        .book{
            display: flex;
            flex-direction: column;
            width: 45%;
        }
        .book-details>p{
            margin-top: 5px;
            font-weight: 500;
            width: 100%;
        }
        .para-heading{
            color: white;
            font-size: 20px;
            font-weight: bolder;
            position: relative;
            top: 20px;
        }
    </style>
</head>
<body>
   <%- include('admin-header') %>
   <p class="para-heading">User's Article</p>
   <div class="user-dash-area" id="book-area">
    <% userarticles.forEach(articles => { %>
    <div class="book">
        <% if(articles.userarticleimg){ %>
            <img src = "<%= articles.userarticleimg %>" alt="Book Image" id="book-img"> 
            <% } %>
       <div class="book-details">
        <p class="book-name book-text-format" style="color: black; width: 100%;"><%= articles.userarticlemssg %></p>
        <form action="update" method="get" style="display: inline-block; margin-top: 10px;">
            <button class="btns" style="background-color: rgb(71, 165, 69);" type="submit">Approve</button>
        </form>
        <form action="/delete-user-article/<%= articles._id %>" method="post" style="display: inline-block; margin-top: 10px;">
            <button class="btns delete-btn" type="submit">Delete</button>
        </form>
       </div>
    </div> <% }) %>
    </div>
    <%- include('footer') %>
    <script src="script.js"></script>
</body>
</html>